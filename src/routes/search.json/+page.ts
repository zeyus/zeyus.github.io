export const prerender = true;
// import type {Builder} from '@sveltejs/kit';

// export function buildSiteContents(builder: Builder) {
//     builder.prerendered.pages.forEach(page => {
//         console.log(page);
//     });
// }
